# SSE Manifest Negative Fixture

This repository contains `mcp.json`, but `server_type` is `sse`.

Expected result:

- "从 GitHub 导入 stdio MCP" should reject this repository.
- Frontend should show a clear type mismatch message.

